diff --git a/codec/common/inc/WelsThreadLib.h b/codec/common/inc/WelsThreadLib.h
--- a/codec/common/inc/WelsThreadLib.h
+++ b/codec/common/inc/WelsThreadLib.h
@@ -50,6 +50,8 @@
 extern "C" {
 #endif
 
-#if defined(_WIN32) || defined(__CYGWIN__)
+#if defined(R4OS_ENCODE)
+#include <r4enc_threads.h>
+#elif defined(_WIN32) || defined(__CYGWIN__)
 
 #include <windows.h>
diff --git a/codec/encoder/core/src/slice_multi_threading.cpp b/codec/encoder/core/src/slice_multi_threading.cpp
--- a/codec/encoder/core/src/slice_multi_threading.cpp
+++ b/codec/encoder/core/src/slice_multi_threading.cpp
@@ -42,6 +42,6 @@
 #include <assert.h>
-#if !defined(_WIN32)
+#if !defined(_WIN32) && !defined(R4OS_ENCODE)
 #include <semaphore.h>
 #include <unistd.h>
 #endif//!_WIN32
 #ifndef SEM_NAME_MAX
@@ -278,7 +278,7 @@
                          "pThreadPEncCtx");
   WELS_VERIFY_RETURN_IF (1, (NULL == pSmt->pThreadPEncCtx))
 
-#ifdef _WIN32
+#if defined(_WIN32) || defined(R4OS_ENCODE)
   // Dummy event namespace, the windows events don't actually use this
   WelsSnprintf (pSmt->eventNamespace, sizeof (pSmt->eventNamespace), "%p", (void*) *ppCtx);
 #else
diff --git a/codec/common/inc/measure_time.h b/codec/common/inc/measure_time.h
--- a/codec/common/inc/measure_time.h
+++ b/codec/common/inc/measure_time.h
@@ -61,7 +61,12 @@
  */
 
+#ifdef R4OS_ENCODE
+int64_t r4enc_monotonic_us(void);
+#endif
 static inline int64_t WelsTime (void) {
-#ifndef _WIN32
+#if defined(R4OS_ENCODE)
+  return r4enc_monotonic_us();
+#elif !defined(_WIN32)
   struct timeval tv_date;
 
   gettimeofday (&tv_date, NULL);
